# FlintTrade — ticks

> High-performance tick processing engine — Rust core with Python bindings via PyO3

## Architecture
- `src/lib.rs` — Rust core (tick simulation, EMA crossover, performance metrics)
- `python/tick_engine/` — Python bindings via PyO3
- Metrics: Sharpe ratio, max drawdown, total return

## Key Classes
- `TickSimulator` — Event-driven tick-level backtesting
- `EMA crossover` — Example strategy implementation in Rust

## Build
```bash
maturin develop          # build and install locally
maturin build --release  # release build
```

## Testing
```bash
python -m pytest packages/core/ticks/tests/ -v --import-mode=importlib
```

## Dependencies

Python bindings expose `tick_engine`; declared build requirements and Rust dependencies live in `packages/core/ticks/pyproject.toml` and `packages/core/ticks/Cargo.toml`.

## Rules
- Read root CLAUDE.md for project-wide rules
- Tests are in the `tests/` directory. Add new test files as needed.
- Update root `changelog.md` only for shipped user-visible or API changes (for example, added/removed exports or signature changes); do not add entries for tests, behaviour-preserving refactors or in-flight design.
- Branch: follow root AGENTS.md; verify live main before opening or merging a PR.
