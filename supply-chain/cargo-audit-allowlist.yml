# cargo-audit warning allowlist.
#
# Vulnerabilities are never suppressed by scripts/cargo-audit-with-allowlist.py.
# Warning-class advisories must carry a reason, owner, and expiry. The PyO3
# tick engine currently has no accepted warning-class advisories.
allowlist: []
