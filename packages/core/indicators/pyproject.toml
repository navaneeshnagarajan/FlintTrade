[project]
name = "flinttrade-indicators"
version = "0.6.0-beta.2"
description = "Technical analysis indicators — pure-NumPy batch (110 exports), streaming classes, Pine Script conversion"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26.0",
    "pydantic>=2.0",
    "numba>=0.59; extra == 'numba'",
]

[project.optional-dependencies]
talib = ["TA-Lib>=0.4"]
numba = ["numba>=0.59"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flinttrade_indicators"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
    "--import-mode=importlib",
    "--strict-markers",
    "-ra",
]
markers = [
    "unit: fast, isolated unit test (default; runs on every commit)",
    "integration: touches DB or mocked external services (PR gate)",
    "slow: long-running test >1s (nightly or via --run-slow)",
]
