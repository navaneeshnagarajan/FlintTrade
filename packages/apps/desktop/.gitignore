# Staged installers
dist-installers/

# Node
node_modules/

# The root .gitignore's macOS custom-folder-icon rule ("Icon?") also matches
# this "icons" directory by accident (? matches the trailing "s"). Force-include
# the Electron-owned application and tray icons.
!resources/icons/
!resources/icons/**
