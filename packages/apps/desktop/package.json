{
  "name": "@flinttrade/desktop",
  "version": "0.6.0-beta.5",
  "private": true,
  "description": "FlintTrade native desktop shell (Tauri 2) — a thin shell that downloads and manages the FlintTrade backend payload in a native window.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
