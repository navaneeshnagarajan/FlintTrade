[project]
name = "flint-automation"
version = "0.5.2-dev"
description = "Cron manager, Telegram bot with kill switch, n8n bridge, post-market analysis, WhatsApp alerter"
requires-python = ">=3.12"
dependencies = [
    "flint-core",
    "flint-ai",
    "httpx>=0.27.0",
    "flask>=3.0",
    "python-telegram-bot>=21.0",
    "apscheduler>=3.10.0",
    "pyotp>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.hatch.build.targets.wheel.sources]
"src" = "flint_automation"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = [
    "--import-mode=importlib",
    "--strict-markers",
    "-ra",
]
markers = [
    "unit: fast, isolated unit test (default; runs on every commit)",
    "integration: touches DB or mocked external services (PR gate)",
    "slow: long-running test >1s (nightly or via --run-slow)",
]
