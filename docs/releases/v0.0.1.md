# FlintTrade v0.0.1

FlintTrade `v0.0.1` is a pre-release. It is not production ready; use Explore and Practice modes before connecting any live broker workflow.

Clean-slate baseline established on 2026-07-23. The earlier `v0.1.0`…`v0.6.0-beta.13` tags and releases (the retired Tauri/PyInstaller line) were deleted so the project could restart with honest, pre-release-marked `0.0.x` semantic versioning while it is still pre-usable. Full development history is preserved in git.

_Release notes pending._
